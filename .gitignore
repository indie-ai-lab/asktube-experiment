# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
build/
dist/

# OS
.DS_Store

# secrets — never commit
.env
.env.*

# Local artifacts
*.md.bak
~*

# Test outputs that shouldn't be tracked
output.md
*.summary.md

# Experiment raw artifacts (kept locally for reference, not in git)
*.mov
*.mp4
*.zip

# Codex sandbox dir is moved into codex-attempt/; the original is local only
/asktube-codex-sandbox/
