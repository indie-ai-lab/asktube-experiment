# Codex attempt

Final state of what Codex (codex-1) produced when handed
[`../README.md`](../README.md) as the only spec, on a fresh empty repo,
allowed to run its full agentic loop (clone → edit → test → iterate → PR).

The code below is unedited from what Codex shipped via PR.
